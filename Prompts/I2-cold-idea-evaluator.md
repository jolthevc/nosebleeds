# I2 · DEPRECATED

This prompt is no longer part of the canonical Nosebleeds Ideation workflow.

Do not call it.

The previous second-model commissioning gate, numeric Idea Score, and 8.5 acceptance threshold were removed because they made Ideation unnecessarily conservative and duplicated the taste function already owned by I1.

Current flow:

OpenAI I1 or I1D
→ deterministic schema validation
→ deterministic deduplication
→ Sheet / UI

If this file remains in the repository for migration safety, treat it as documentation only.
